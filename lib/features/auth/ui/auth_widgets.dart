import 'package:flutter/material.dart';

/// Reusable auth-feature widgets (forms, buttons, etc.).
class AuthFormPlaceholder extends StatelessWidget {
  const AuthFormPlaceholder({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
