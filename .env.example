# Copy to `.env.local` (gitignored) for local emulator / scripts.
# Production: Firebase Secret `GEMINI_API_KEY`
#
# Flutter app never embeds these keys — Vision OCR goes through Cloud Function.

GEMINI_API_KEY=
GEMINI_VISION_API_KEY=
